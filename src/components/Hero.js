import React from "react";

const Hero = props =>
  <section className="hero is-medium is-primary is-bold">
    <div className="hero-body">
      <div className="container">
        <h1 className="title">React &amp; Redux Weather App</h1>
        <h2 className="subtitle">Search 5 days weather by City name.</h2>
      </div>
    </div>
  </section>;

export default Hero;
